from setuptools import setup

setup(name='gym_maze',
      version='0.0.1',
      install_requires=['gym']  
     )

setup(name='gym_maze_1',
      version='0.0.1',
      install_requires=['gym']
     )

setup(name='gym_maze_2',
      version='0.0.1',
      install_requires=['gym']
     )
